#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../google-gax@3.6.0/node_modules/google-gax/build/tools/compileProtos.js" "$@"
else
  exec node  "$basedir/../../../../../../google-gax@3.6.0/node_modules/google-gax/build/tools/compileProtos.js" "$@"
fi
